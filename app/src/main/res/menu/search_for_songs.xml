<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:title="search ..."
        android:id="@+id/search_option"
        android:icon="@drawable/song_search"
        app:showAsAction="ifRoom"
        app:actionViewClass="androidx.appcompat.widget.SearchView" />

    <item
        android:title="Sort By"
        android:id="@+id/sort_option"
        app:showAsAction="never">
        <menu>
            <item android:title="Name"
                android:id="@+id/sortByName"
                app:showAsAction="never"
                android:background="@color/Note3"/> <!-- Change the text color here -->
            <item android:title="Date"
                android:id="@+id/sortByDate"
                app:showAsAction="never"/>
            <item android:title="Size"
                android:id="@+id/sortBySize"
                app:showAsAction="never"/>

        </menu>

    </item>
</menu>